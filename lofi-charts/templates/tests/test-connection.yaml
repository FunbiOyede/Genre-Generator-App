apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lofi-charts.fullname" . }}-test-connection"
  labels:
    {{- include "lofi-charts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lofi-charts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
