apiVersion: v1
kind: Service
metadata:
  name: {{ include "lofi-charts.fullname" . }}
  labels:
    {{- include "lofi-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lofi-charts.selectorLabels" . | nindent 4 }}
